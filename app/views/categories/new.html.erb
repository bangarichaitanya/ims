<h1>New Category</h1>

<%= form_for(@category) do |f| %>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :parent_id %><br />
    <%= f.select :parent_id, options_for_select(Category.parent_categories.map { |c| [c.name, c.id] }), { :include_blank => true } %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', categories_path %>
