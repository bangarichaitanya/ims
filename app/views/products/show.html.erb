<p>
  <b>Name:</b>
  <%=h @product.name %>
</p>

<p>
  <b>Category:</b>
  <%=h @product.category.name ? "#{@product.category.parent.name} - #{@product.category.name}" : "N/A"  %>
</p>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>